#pragma once

// 사칙 연산 함수 선언
// 사칙 연산 함수를 
// 헤더 파일(Math.h)에서는 함수 선언
// 소스 파일(Math.c)에는 함수 정의
// 로 나누어서 작성!

// 사칙 연산 함수
int Add(int a, int b);
int Sub(int a, int b);
int Mul(int a, int b);
int Div(int a, int b);
